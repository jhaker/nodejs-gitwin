# gitwin - git windows for node.js

### example - pull
```
	var gitwin = require('gitwin');
	gitwin.path = "c:/mygitrepo";
	gitwin.pull();
};
```

### example - status
```
	var gitwin = require('gitwin');
	gitwin.path = "c:/mygitrepo";
	gitwin.status();
};
```